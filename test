<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>FlowState — Technical Study Monitor</title>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700;800&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet"/>
<style>
  :root {
    --green: #00FF41; 
    --bg: #050505;
    --bg2: #0F0F0F;
    --border: #222;
    --text: #E0E0E0;
    --text-dim: #666;
    --font-main: 'Inter', sans-serif;
    --font-mono: 'JetBrains Mono', monospace;
  }

  * { margin: 0; padding: 0; box-sizing: border-box; -webkit-font-smoothing: antialiased; }

  body {
    background: var(--bg);
    color: var(--text);
    font-family: var(--font-main);
    line-height: 1.5;
    letter-spacing: -0.01em;
    overflow-x: hidden;
  }

  /* --- NAVIGATION --- */
  nav {
    position: fixed; top: 0; width: 100%; z-index: 100;
    display: flex; align-items: center; justify-content: space-between;
    padding: 1rem 2rem;
    background: rgba(5,5,5,0.8);
    backdrop-filter: blur(8px);
    border-bottom: 1px solid var(--border);
  }
  .nav-logo { font-family: var(--font-mono); font-weight: 700; font-size: 14px; color: var(--green); }
  .nav-links { display: flex; gap: 1.5rem; }
  .nav-links a { color: var(--text-dim); text-decoration: none; font-size: 12px; font-family: var(--font-mono); transition: color 0.2s; }
  .nav-links a:hover { color: var(--text); }

  /* --- HERO SECTION --- */
  .hero {
    height: 85vh;
    display: flex; flex-direction: column; align-items: center; justify-content: center;
    text-align: center; border-bottom: 1px solid var(--border);
    padding: 0 1rem;
  }
  .hero h1 {
    font-size: clamp(3.5rem, 10vw, 8rem);
    font-weight: 800; letter-spacing: -0.04em; line-height: 0.85;
    margin-bottom: 2rem; text-transform: uppercase;
  }
  .hero-sub {
    font-family: var(--font-mono); font-size: 14px; color: var(--text-dim);
    max-width: 500px; margin-bottom: 3rem;
  }

  /* --- MAIN CONTENT GRID --- */
  .grid-container {
    display: grid;
    grid-template-columns: 1fr 350px;
    gap: 0;
    border-bottom: 1px solid var(--border);
  }
  .main-panel { padding: 4rem 2rem; border-right: 1px solid var(--border); }
  .side-panel { padding: 2.5rem; background: var(--bg2); position: sticky; top: 60px; height: calc(100vh - 60px); }

  .label {
    font-family: var(--font-mono); font-size: 11px; color: var(--green);
    text-transform: uppercase; margin-bottom: 1.5rem; display: block;
    letter-spacing: 0.1em;
  }

  /* --- TELEMETRY STATS --- */
  .stat-row {
    display: flex; justify-content: space-between; padding: 14px 0;
    border-bottom: 1px solid var(--border);
    font-family: var(--font-mono); font-size: 12px;
  }
  .stat-row span:last-child { color: var(--text-dim); }
  .stat-row.active span:last-child { color: var(--green); }

  /* --- BUTTONS --- */
  .btn {
    font-family: var(--font-mono); font-size: 13px; font-weight: 500;
    padding: 12px 24px; border: 1px solid var(--text);
    background: transparent; color: var(--text); cursor: pointer;
    transition: all 0.2s; border-radius: 2px;
  }
  .btn:hover { background: var(--text); color: var(--bg); }
  .btn-green { border-color: var(--green); color: var(--green); }
  .btn-green:hover { background: var(--green); color: var(--bg); }

  /* --- VIDEO PREVIEW --- */
  #video-container {
    width: 100%; aspect-ratio: 4/3;
    background: #000; border: 1px solid var(--border);
    margin-bottom: 2rem; position: relative; overflow: hidden;
  }
  video { width: 100%; height: 100%; object-fit: cover; filter: grayscale(100%) contrast(1.2); opacity: 0.7; }
  .video-overlay {
    position: absolute; inset: 0;
    border: 1px solid rgba(0, 255, 65, 0.2);
    pointer-events: none;
    background: repeating-linear-gradient(0deg, rgba(0,0,0,0.05) 0px, rgba(0,0,0,0.05) 1px, transparent 1px, transparent 2px);
  }

  /* --- STATES LIST --- */
  .state-item {
    padding: 2rem 0; border-top: 1px solid var(--border);
    display: grid; grid-template-columns: 150px 1fr;
    gap: 20px;
  }
  .state-name { font-family: var(--font-mono); font-weight: 700; font-size: 13px; letter-spacing: 0.05em; }
  .state-desc { color: var(--text-dim); font-size: 15px; max-width: 500px; }

  footer {
    padding: 2rem; border-top: 1px solid var(--border);
    font-family: var(--font-mono); font-size: 10px; color: var(--text-dim);
    display: flex; justify-content: space-between;
  }

  @media (max-width: 900px) {
    .grid-container { grid-template-columns: 1fr; }
    .main-panel { border-right: none; }
    .side-panel { position: relative; top: 0; height: auto; }
    .state-item { grid-template-columns: 1fr; gap: 10px; }
  }
</style>
</head>
<body>

<nav>
  <div class="nav-logo">FLOWSTATE_V0.1</div>
  <div class="nav-links">
    <a href="#logic">LOGIC</a>
    <a href="#monitor">MONITOR</a>
    <a href="https://github.com" target="_blank">SOURCE</a>
  </div>
  <button class="btn" style="padding: 6px 12px;" onclick="initCamera()">INITIALIZE_SENSORS</button>
</nav>

<div class="hero">
  <span class="label">// AUTONOMOUS_FOCUS_MONITOR</span>
  <h1>FOCUS IS<br/>A SIGNAL.</h1>
  <p class="hero-sub">Deep-work telemetry for the Digital SAT generation. We track metabolic and behavioral data to prevent unproductive struggle.</p>
  <div style="display: flex; gap: 15px;">
    <button class="btn btn-green" onclick="document.getElementById('monitor').scrollIntoView({behavior:'smooth'})">START_MONITOR</button>
    <button class="btn">VIEW_DOCUMENTATION</button>
  </div>
</div>

<div class="grid-container" id="logic">
  <div class="main-panel">
    <span class="label">01 // DETECTION_LOGIC</span>
    <h2 style="font-size: 3.5rem; margin-bottom: 3rem; font-weight: 800; letter-spacing: -0.04em;">SYSTEM_STATES</h2>
    
    <div class="state-item">
      <div class="state-name" style="color:var(--green)">[ 01 ] FLOW</div>
      <div class="state-desc">Rhythmic keystrokes. Constant gaze fixation. System remains in passive observation mode.</div>
    </div>
    <div class="state-item">
      <div class="state-name" style="color:orange">[ 02 ] DRIFT</div>
      <div class="state-desc">Frequent tab switching. Erratic scroll patterns. System issues a low-latency "haptic" nudge.</div>
    </div>
    <div class="state-item">
      <div class="state-name" style="color:red">[ 03 ] STUCK</div>
      <div class="state-desc">High backspace frequency (>15/min) + idle time. System triggers contextual AI intervention.</div>
    </div>
  </div>

  <div class="side-panel" id="monitor">
    <span class="label">LIVE_TELEMETRY</span>
    
    <div id="video-container">
      <video id="webcam" autoplay playsinline></video>
      <div class="video-overlay"></div>
    </div>

    <div class="stat-row active"><span>SENSOR_STATUS</span><span id="sensor-status">STANDBY</span></div>
    <div class="stat-row"><span>GAZE_FIXATION</span><span id="gaze-val">NULL</span></div>
    <div class="stat-row"><span>KEYSTROKE_CPM</span><span id="cpm-val">000</span></div>
    <div class="stat-row"><span>IDLE_TIMER</span><span id="idle-val">00:00s</span></div>
    
    <div style="margin-top: 4rem;">
      <span class="label">AGGREGATE_FOCUS_SCORE</span>
      <div id="score-display" style="font-size: 5rem; font-weight: 800; font-family: var(--font-mono); color: var(--text-dim);">--%</div>
      <div style="height: 2px; background: var(--border); margin-top: 15px;">
        <div id="score-bar" style="width: 0%; height: 100%; background: var(--green); transition: width 0.5s;"></div>
      </div>
    </div>
  </div>
</div>

<footer>
  <span>NODE: IRVINGTON_HIGH_SENSORS</span>
  <span>EDTECH_TRACK_2026</span>
  <span>PRIVACY: LOCAL_ONLY</span>
</footer>

<script>
  // INITIALIZE CAMERA SENSORS
  async function initCamera() {
    const video = document.getElementById('webcam');
    const status = document.getElementById('sensor-status');
    
    try {
      const stream = await navigator.mediaDevices.getUserMedia({ video: true });
      video.srcObject = stream;
      status.innerText = "ACTIVE";
      status.style.color = "var(--green)";
      startSimulation();
    } catch (err) {
      console.error("Camera Error: ", err);
      status.innerText = "DENIED";
      status.style.color = "red";
      alert("Camera access is required for live telemetry.");
    }
  }

  // SIMULATE LIVE DATA UPDATES
  function startSimulation() {
    const scoreText = document.getElementById('score-display');
    const scoreBar = document.getElementById('score-bar');
    const gazeVal = document.getElementById('gaze-val');
    const cpmVal = document.getElementById('cpm-val');
    
    setInterval(() => {
      // Create some "jitter" for realism
      const randomScore = Math.floor(Math.random() * (95 - 80 + 1)) + 80;
      const randomCPM = Math.floor(Math.random() * (450 - 380 + 1)) + 380;
      
      scoreText.innerText = randomScore + "%";
      scoreText.style.color = "var(--green)";
      scoreBar.style.width = randomScore + "%";
      gazeVal.innerText = "STABLE";
      cpmVal.innerText = randomCPM;
    }, 2000);
  }

  // TRACK IDLE TIME
  let idleTime = 0;
  setInterval(() => {
    idleTime++;
    document.getElementById('idle-val').innerText = `00:${idleTime.toString().padStart(2, '0')}s`;
  }, 1000);

  // RESET IDLE ON INTERACTION
  document.onmousemove = () => idleTime = 0;
  document.onkeydown = () => idleTime = 0;
</script>

</body>
</html>
